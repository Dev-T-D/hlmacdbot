pandas==2.3.3
numpy==2.3.4
requests==2.31.0
python-dateutil==2.8.2

# Hyperliquid dependencies (for wallet-based signing)
eth-account>=0.10.0
web3>=6.0.0
eth-utils>=2.0.0

# Optional: Official Hyperliquid Python SDK (not required for this bot)
# hyperliquid-python-sdk>=0.3.0
# GitHub: https://github.com/hyperliquid-dex/hyperliquid-python-sdk
# Note: This bot uses a custom implementation for interface compatibility

# Optional: Health monitor endpoint (requires Flask)
# flask>=2.0.0
# Install with: pip install flask

# Optional: Secure credential storage (requires keyring)
# keyring>=23.0.0
# Install with: pip install keyring
# Note: Falls back to environment variables/config file if not installed

# Secure key storage (for in-memory encryption)
cryptography>=41.0.0

# WebSocket support for real-time data (optional)
websockets>=12.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Machine Learning dependencies
lightgbm>=4.0.0
xgboost>=1.7.0
tensorflow>=2.13.0  # Optional: for LSTM models
shap>=0.42.0        # Optional: for model explainability
optuna>=3.3.0       # Optional: for hyperparameter optimization

# ONNX dependencies for ultra-fast ML inference
onnxmltools>=1.11.0  # Model conversion to ONNX
onnxruntime>=1.15.0  # Optimized ONNX runtime
numba>=0.57.0        # JIT compilation for feature engineering

# Async HTTP client for high-performance API calls
aiohttp>=3.9.0

# Redis for high-performance caching (optional)
redis>=4.5.0

# System monitoring for performance benchmarks
psutil>=5.9.0

# Monitoring and observability
prometheus_client>=0.19.0

# Alerting channels
python-telegram-bot>=20.0
discord-webhook>=1.3.0

# HTTP server for health checks
flask>=3.0.0

# PDF report generation
reportlab>=4.0.0

